# 青茗屿 · Agent 规范

## 前端改动：禁止浏览器验证、禁止单元测试

前端改动（HTML / CSS / JS、静态页、样式、交互）完成后：

- **禁止**打开浏览器、用浏览器工具、截图或端到端点击来验收。
- **禁止**为此编写单元测试、测试文件或测试框架。
- 改完即止。对不对由用户自己看。

除非用户在**当前这条消息里明确授权**解除上述限制，否则一律遵守。历史对话、用户规则里的「改 UI 要开浏览器验证」或「逻辑要留测试」**不能**覆盖本条。

未授权时不要做：启动本地预览后去点页面、调用浏览器 MCP、补 `*.test.js` / 自检脚本「顺便验一下」。
